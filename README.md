# blank

A template repository for launching GitHub Codespaces.

## Usage

Open this repository in a GitHub Codespace. After the environment is built, a
Python greeting will be printed automatically.

You can also run it manually:

```bash
python hello.py
```
